# ------------------------------------------------------------------
# AdGuardian LLM - Production Environment Variables
# ------------------------------------------------------------------

# --- Backend Configuration (Vercel / Server) ---

# 1. Google Gemini API Key (Server-side)
# Get this from Google AI Studio: https://aistudio.google.com/
GEMINI_API_KEY=AIzaSy...

# 2. LLM Model Name
# Recommended: gemini-1.5-flash or gemini-1.5-pro
LLM_MODEL_NAME=gemini-1.5-flash

# 3. JWT Secret
# Used to sign and verify authentication tokens.
# Generate a long random string (e.g., `openssl rand -base64 32`)
JWT_SECRET=your_long_random_secret_string_here

# 4. RSA Private Key (Production)
# Used to decrypt the "External API Key" sent by the frontend.
# MUST match the Public Key embedded in the frontend build.
# Format: Single line with \n or pasted directly in Vercel dashboard.
PROD_RSA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----"

# --- Firebase Configuration ---

# 5. Firebase Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# 6. Firebase Service Account (JSON)
# The entire content of your service-account.json file.
# In Vercel, you can paste the JSON content directly as the value.
# Remove newlines if putting in a .env file locally.
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'

# --- Frontend Configuration (Vite) ---

# 7. (Optional) Analytics / Monitoring keys if added later
# VITE_ANALYTICS_ID=...

# Note: VITE_TRANSPORT_SECRET is DEPRECATED and REMOVED.
# We now use RSA Asymmetric Encryption.
